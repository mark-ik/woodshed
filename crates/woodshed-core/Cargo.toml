[package]
name = "woodshed-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Woodshed's portable state/logic core: app state over the woodshedding theory crate, no UI or host dependency. The serval-host plan's W1.1 split; S1 ships the Stage slice."

[dependencies]
woodshedding = { workspace = true }
woodshed-graph = { workspace = true }
serde = { workspace = true }
# Persona-keyed encrypt-at-rest for the private practice session — the woodshed
# pole of the personae suite adoption (one persona, many devices, sealed state).
# Sibling repo, like woodshed-graph's chartulary path dep.
personae = { path = "../../../personae" }
hex = "0.4"

[dev-dependencies]
serde_json = { workspace = true }
