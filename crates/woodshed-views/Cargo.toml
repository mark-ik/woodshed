[package]
name = "woodshed-views"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Woodshed's xilem_serval view layer: view fns + CSS sheets for the serval host (desktop and web). Serval-host plan S0+."

[dependencies]
woodshed-core = { workspace = true }
xilem-serval = { workspace = true }
tinct = { workspace = true }
