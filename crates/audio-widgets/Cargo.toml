[package]
name = "audio-widgets"
version = "0.1.0"
description = "Shared Masonry/Vello audio widgets (waveform, …) for Woodshed, Strophe, and Mere."
edition = "2024"
rust-version = "1.92"
license.workspace = true
authors.workspace = true
repository.workspace = true
publish = false

[dependencies]
xilem = { workspace = true }
masonry = { workspace = true }
# Theme: `ThemeMode` round-trips through a host's settings.
serde = { workspace = true }
